server {
        listen 80 default_server;
        listen [::]:80 default_server;
        root /home/ubuntu/static-site;
        server_name _;
        location /basic_status  {
                stub_status;
                try_files $uri $uri/ =404;
        }
}
